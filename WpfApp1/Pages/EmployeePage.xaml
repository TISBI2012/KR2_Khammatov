<Page x:Class="WpfApp1.Pages.EmployeePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WpfApp1.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="EmployeePage">

    <Grid Background="#fff">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0">
            <TextBlock Text="Добро пожаловать!"
                       FontSize="40px" Margin="0 0 0 20"/>
            <TextBlock Text="Ваша роль: сотрудник отдела"
                       FontSize="20px" Margin="0 0 0 20"/>
            <Button Content="Посмотреть список задач"
                    FontSize="20px"
                    Name="RequestEnployeeBt"
                    Click="RequestEnployeeBt_Click"/>
            <Button Content="Информация об аккаунте"
                    FontSize="20px"
                    Margin="0 20 0 0"/>
            <Button Content="Помоги"
                    FontSize="20px"
                    Margin="0 20 0 0"/>
        </StackPanel>
        <Frame Name="MainFrame" Grid.Column="1"/>
    </Grid>
</Page>
